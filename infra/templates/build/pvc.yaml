apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sources
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.sourcesPVC.storageClass }}
  resources:
    requests:
      storage: 2Gi
